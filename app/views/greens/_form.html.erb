<%= form_with(model: green, local: true) do |form| %>
  <% if green.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(green.errors.count, "error") %> prohibited this green from being saved:</h2>

      <ul>
        <% green.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :sow_period %>
    <%= form.text_field :sow_period %>
  </div>

  <div class="field">
    <%= form.label :grow_period %>
    <%= form.text_field :grow_period %>
  </div>

  <div class="field">
    <%= form.label :harvest_period %>
    <%= form.text_field :harvest_period %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
